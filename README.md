# Habit Tracker

A single-page weekly habit tracker built with Vite and React.

Users can add, rename, and delete habits, check off each day on a weekly grid, navigate between weeks, jump to a specific week with a date picker, choose a theme color, and keep their data after refreshing the page.

## How to run locally

Make sure Node.js is installed.

```bash
npm install
npm run dev